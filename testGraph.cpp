#include <iostream>
#include <vector>

#include "GraphClass.hpp"

using namespace std;

int main() {

  vertex a = vertex("A");
  vertex b = vertex("B");
  vertex c = vertex("C");
  vertex d = vertex("D");
  addVertex(a);

  return 0;
}
